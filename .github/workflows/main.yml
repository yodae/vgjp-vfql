name: hjkhlljnljjj
# on: [push, pull_request]
on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:
   
jobs:
  build:
    runs-on: macos-latest
    strategy:
      max-parallel: 20
      fail-fast: false
      matrix:
        go: [1.0]
        flag: [A]
    timeout-minutes: 5
    env:
        NUM_JOBS: 900
        JOB: ${{ matrix.go }}
    steps:
      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16.x'
      - name: Open page in Chrome
        uses: sdras/browser-actions@v1
        with:
          browserType: chrome
          url: https://yoelmod.blogspot.com/2022/02/disney-premium-mod-apk-para-android.html
          viewport: '{ "width": 1920, "height": 1080 }'
          zoom: 30
