name: hjkhlljnljjj
# on: [push, pull_request]
on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:
   
jobs:
  build:
    runs-on: macos-latest
    strategy:
      max-parallel: 20
      fail-fast: false
      matrix:
        go: [1.0]
        flag: [A]
    timeout-minutes: 5
    env:
        NUM_JOBS: 900
        JOB: ${{ matrix.go }}
    steps:
      - name: ok
        run: open -a --args --force-device-scale-factor=0.3 "Google Chrome" "https://yoelmod.blogspot.com/2022/02/disney-premium-mod-apk-para-android.html"
